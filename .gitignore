# Ruido de macOS
.DS_Store
**/.DS_Store

# Configuración local de herramientas
.claude/settings.local.json
*.local.json

# Secretos / variables de entorno (NUNCA subir)
.env
.env.*
# ...pero sí versionamos la plantilla de ejemplo
!.env.example

# Archivos temporales de Office
~$*
.~lock.*
